autogenerated = 0 # remove this line to remove the warning

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/Configuring-Hyprland/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf

source = ~/.config/hypr/mocha.conf

################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=eDP-1,preferred,auto,1


###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$fileManager = $terminal lf
$menu = PATH=${PATH}:${HOME}/.local/bin bemenu-run -H 25


#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = QT_QPA_PLATFORM,wayland;xbc
env = GTK_THEME,catppuccin-mocha-red-standard+default
env = XCURSOR_SIZE,22
env = XCURSOR_THEME,breeze_cursors
env = HYPRCURSOR_SIZE,22

#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

exec-once = uwsm app -- /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec-once = uwsm app -- hyprpaper
exec-once = uwsm app -- $HOME/.local/bin/hprw
exec-once = uwsm app -- waybar
exec-once = uwsm app -- udiskie -s -A --menu-update-workaround
exec-once = uwsm app -- wl-paste --type text --watch cliphist store
exec-once = uwsm app -- wl-paste --type image --watch cliphist store
exec-once = uwsm app -- /usr/bin/noisetorch -i -s alsa_input.pci-0000_00_1f.3.analog-stereo -t 95
exec-once = [workspace 1 silent] kitty
exec-once = [workspace 6 silent] keepassxc 

#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 2
    gaps_out = 2

    border_size = 1

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = $lavender 
    col.inactive_border = $overlay1 

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = $overlay2 
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 3
        passes = 1

        vibrancy = 0.1696
    }
}

ecosystem {
  no_update_news = true
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = true

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us,ru
    #kb_variant = lang
    #kb_model =
    kb_options = grp:caps_toggle, compose:ralt
    #kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    force_no_accel = true
    accel_profile = flat

    touchpad {
        natural_scroll = false
        disable_while_typing = false
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    workspace_swipe_invert = false
    workspace_swipe_distance = 200
    workspace_swipe_forever = true
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, RETURN, exec, $terminal
bind = $mainMod, C, killactive,
bind = $mainMod SHIFT, Q, exit,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, SPACE, togglefloating,
bind = $mainMod, R, exec, $menu 
bind = $mainMod, P, exec, hyprpicker | wl-copy
#bind = $mainMod, P, pseudo, # dwindle
#bind = $mainMod, J, togglesplit, # dwindle
bind = $mainMod, F, fullscreen
bind = $mainMod, Print, exec, grim -g "$(slurp)" ~/Pictures/Screenshots/screen-$(date +%s).png
bind = , Print, exec, grim ~/Pictures/Screenshots/screen-$(date +%s).png
bind = $mainMod, V, exec, cliphist list | bemenu -H 25 | cliphist decode | wl-copy
bind = $mainMod, W, exec, ~/.local/bin/hprw

# Move focus with mainMod + arrow keys
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, j, movefocus, u
bind = $mainMod, k, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl s 10%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl s 10%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

windowrule = workspace 1, match:class (kitty)

windowrule {
    # Ignore maximize requests from all apps. You'll probably like this.
    name = suppress-maximize-events
    match:class = .*

    suppress_event = maximize
}
windowrule {
    # Fix some dragging issues with XWayland
    name = fix-xwayland-drags
    match:class = ^$
    match:title = ^$
    match:xwayland = true
    match:float = true
    match:fullscreen = false
    match:pin = false

    no_focus = true
}
# Hyprland-run windowrule
windowrule {
    name = move-hyprland-run

    match:class = hyprland-run

    move = 20 monitor_h-120
    float = yes
}
# waybar
windowrule = stay_focused on, match:title (),match:class (waybar)
windowrule = min_size 1 1, match:title (),match:class (waybar)
# MEGAsync
windowrule = float on,match:class (nz.co.mega.megasync)
windowrule = size 402 564,match:title (MEGAsync),match:class (nz.co.mega.megasync)
windowrule = no_blur on,match:title (MEGAsync),match:class (nz.co.mega.megasync)
windowrule = rounding 16,match:title (MEGAsync),match:class (nz.co.mega.megasync)
# discord
windowrule = workspace 8, match:class (discord),match:title (.*)(Discord)
windowrule = float on, match:class (discord)
windowrule = tile on, match:class (discord),match:title (.*)(Discord)
# keepassxc
windowrule = float on, match:title (KeePassXC -)(.*)
windowrule = workspace 6, match:initial_class (org.keepassxc.KeePassXC),match:initial_title (.*)(KeePassXC)
# thunderbird
windowrule = float on, match:class (thunderbird)
windowrule = tile on, match:initial_class (thunderbird),match:initial_title (.*)(Mozilla Thunderbird)
# file picker
windowrule = float on, match:title (Open Files)
# blueman-manager
windowrule = float on, match:initial_class (blueman-manager),match:initial_title (Bluetooth Devices)
windowrule = size -50% 50%, match:initial_class (blueman-manager),match:initial_title (Bluetooth Devices)
windowrule = move 102% 3%, match:initial_class (blueman-manager),match:initial_title (Bluetooth Devices)
# pwucontrol
windowrule = float on, match:class (com.saivert.pwvucontrol),match:title (Pipewire Volume Control)
windowrule = size 43% 37.037037037037037037%, match:class (com.saivert.pwvucontrol),match:title (Pipewire Volume Control)
windowrule = move 56.68% 2.9%, match:class (com.saivert.pwvucontrol),match:title (Pipewire Volume Control)
# furryfox
windowrule = workspace 2, match:class (waterfox)(.*)
windowrule = float on, match:class (waterfox),match:title (.*)
windowrule = tile on, match:class (waterfox),match:title (.*)(Waterfox)(.*)
windowrule = float on, match:class (waterfox),match:title (Picture-in-Picture)
windowrule = pin on, match:class (waterfox),match:title (Picture-in-Picture)
windowrule = move 65.3% 65%, match:class (waterfox),match:title (Picture-in-Picture)
# thunderbird
windowrule = workspace 4, match:class (thunderbird)
# kleopatra
windowrule = workspace 6, match:class (org.kde.kleopatra)
# creative shit
windowrule = workspace 5, match:class (gimp)
windowrule = workspace 5, match:class (krita)
windowrule = workspace 5, match:class (blender)
# qbittorrent
windowrule = workspace 7, match:class (org.qbittorrent.qBittorrent),match:title (qBittorrent )(.*)
windowrule = float on, match:class (org.qbittorrent.qBittorrent),match:title (.*)
windowrule = tile on, match:class (org.qbittorrent.qBittorrent),match:title (qBittorrent )(.*)
# ivm 
windowrule = float on, match:class (imv),match:title (imv)(.*)
# davinci
windowrule = float on, match:class (resolve),match:initial_title (Fusion Reactor)
windowrule = stay_focused on, match:class (resolve),match:initial_title (Dialog)
# gimp
windowrule = float on, match:title (Export Image as)(.*)
# gaming
# steam
windowrule = size +20% 75%,match:class (steam),match:title (Friends List)
windowrule = move 40% 13.6574074074074074074%,match:class (steam),match:title (Friends List)
windowrule = stay_focused on, match:title (),match:class (steam)
windowrule = min_size 1 1, match:title (),match:class (steam)
windowrule = float on, match:class (steam),match:title (.*)
windowrule = pin on, match:class (steam),match:title (notificationtoasts)(.*)(desktop)
windowrule = tile on, match:initial_class (steam),match:initial_title (Steam)
windowrule = workspace 10, match:class (steam_app_)(.*),match:title (.*)
windowrule = fullscreen on, match:class (steam_app_)(.*),match:title (.*)
windowrule = workspace 9, match:initial_class (steam),match:initial_title (Steam)
windowrule = float on, match:class (Steam),match:title (Pick game to add)
# paradox 
windowrule = float on, match:class (paradox-launcher-v2)
windowrule = size 1280 670, match:class (paradox-launcher-v2)
windowrule = workspace 10, match:initial_class (paradox-launcher-v2)
windowrule = workspace 10, match:class (Cities.x64)
windowrule = workspace 10, match:class (hoi4)
# minecraft
windowrule = float on, match:class (org.prismlauncher.PrismLauncher),match:title (.*)( Prism Launcher)(.*)
windowrule = workspace 9, match:class (org.prismlauncher.PrismLauncher),match:title (Prism Launcher )(.*)
windowrule = workspace 10, match:initial_class (.*)(Minecraft)(.*),match:initial_title (.*)(Minecraft)(.*)
windowrule = maximize on, match:initial_class (.*)(Minecraft)(.*),match:initial_title (.*)(Minecraft)(.*)
# other
windowrule = workspace 10, match:class (factorio)
windowrule = workspace 10, match:title (Vintage Story)
