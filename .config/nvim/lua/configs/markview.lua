---@type markview.config
return {
  experimental = {
    check_rtp = false,
  },
  latex = {
    enable = false,
    blocks = {
        enable = false,
    },
    inlines = {
      enable = false,
    },
  }
}
