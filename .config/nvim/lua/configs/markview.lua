---@type markview.config
return {
  experimental = {
    check_rtp = false,
  },
  latex = {
    enable = true,
    blocks = {
        enable = true,
    },
    inlines = {
      enable = true,
    },
  }
}
